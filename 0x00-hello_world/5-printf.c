#include <stdio.h>
/**
 * main -  the entry point in C
 *
 * Description:the program has  a printf that prints an output
 * Return: 0 that means there are no errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
