#include "main.h"
/**
 * _strcat - entry point of the program
 * @dest: the first string
 * @srch: the second string
 * Description: this program concatenates two strings
 * Return: char
 */
char *_strcat(char *dest, char *srch)
{
	strcat(dest, srch);
	return (dest);


}

