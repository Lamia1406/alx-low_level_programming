#include "main.h"
/**
 * _strlen - entry point of the program
 * @s: the input string
 * Description: this program returns the length of a string
 * Return: int (length)
 */
int _strlen(char *s)
{
	return (strlen(s));
}

